Makes Enter select multiple setup features through an explicit Continue action.

## Changes

- Pressing `Enter` on a Java feature now selects or unselects that feature without leaving the menu.
- The multi-select menu includes a visible `[ Continue ]` row that submits the complete selection.
- Space remains available as a secondary toggle shortcut.

## Validation

The interactive state behavior and complete BetterGit test suite pass with the rebuilt executable JAR.
